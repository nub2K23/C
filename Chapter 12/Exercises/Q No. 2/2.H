#define AVG(x, y) ((x + y) / 2 )
#define MOD(x) (x >= 0 ? x : x * -1 )
#define CONVERTALPHA(ch) ((ch >= 'a' && ch <= 'z') ? (ch - 'a' + 'A') : ch )
#define BIGNUM(x, y, z) (x > y ? x : (y > z ? y : z) )
